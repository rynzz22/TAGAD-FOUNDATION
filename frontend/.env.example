# Frontend Environment Variables
# Frontend variables are SAFE for browser exposure (use VITE_ prefix)

# API Base URL (required for frontend to communicate with backend)
VITE_API_BASE_URL=http://localhost:3000/api

# Optional: Supabase (if used in frontend)
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
